spring.application.name=admin-server

eureka.client.enabled=${EUREKA_CLIENT_ENABLED:true}
eureka.client.serviceUrl.defaultZone=${EUREKA_SERVICE_URL:http://localhost:8761}/eureka/

spring.cloud.config.enabled=${CONFIG_DISCOVERY_ENABLED:false}
spring.cloud.config.discovery.serviceId=${CONFIG_DISCOVERY_SERVICE_ID:config-server}
spring.cloud.config.discovery.enabled=${CONFIG_DISCOVERY_ENABLED:false}
spring.cloud.config.failFast=${CONFIG_FAIL_FAST:false}